# DDoS Load Tool Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
aiohttp                 # Async HTTP client for high-performance requests
requests>=2.31.0        # HTTP library for basic load tester

# Optional but recommended for better performance
aiodns                  # Async DNS resolver for faster lookups
chardet                 # Fast character encoding detection
Brotli>=1.1.0           # Brotli compression support
